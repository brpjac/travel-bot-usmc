# LLM (plain Google SDK — structured output + reliable usage metadata)
google-genai>=1.0.0
pydantic>=2.0

# FAISS deep-search fallback (LangChain kept only for the vectorstore format)
langchain-community>=0.3.0
langchain-huggingface>=0.1.0
langchain-text-splitters>=0.3.0
sentence-transformers>=3.0.0
faiss-cpu>=1.9.0

# Ingestion
pymupdf>=1.25.0
pyyaml>=6.0

# Frontend
streamlit>=1.40.0
