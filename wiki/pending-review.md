# Pending Review

Staging queue for claims that could not be merged into `_claims.yml` with
confidence — conflicting sources, ambiguous provenance, or unverifiable
numbers. `/ingest` adds entries here; a human resolves them (merge with a C###
or discard) and deletes the entry.

_Empty. Nothing pending._
